services:
  veloxdb:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: veloxdb:dev
    ports:
      - "6379:6379"
    volumes:
      - veloxdb-data:/data
    environment:
      VELOXDB_AOF_ENABLED: "true"
      VELOXDB_AOF_PATH: "/data/veloxdb.aof"

volumes:
  veloxdb-data:
